<div class="page-header">
  <h1>
    Lists
    <a href="/lists/new" class="btn btn-lg btn-default">New List</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Title</th>
        <th>Board</th>
        <th>Position order</th>
        <th>Actions</th>
      </tr>

      <% @lists.each do |list| %>
      <tr>
        <td><%= list.title %></td>
        <td><%= list.board_id %></td>
        <td><%= list.position_order %></td>
        <td>
          <a href="/lists/<%= list.id %>" class="btn btn-default">Show</a>
          <a href="/lists/<%= list.id %>/edit" class="btn btn-default">Edit</a>
          <a href="/delete_list/<%= list.id %>" class="btn btn-default" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
