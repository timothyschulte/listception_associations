<div class="page-header">
  <h1>
    Cards
    <a href="/cards/new" class="btn btn-lg btn-default">New Card</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>List</th>
        <th>Position order</th>
        <th>Actions</th>
      </tr>

      <% @cards.each do |card| %>
      <tr>
        <td><%= card.title %></td>
        <td><%= card.description %></td>
        <td><%= card.list_id %></td>
        <td><%= card.position_order %></td>
        <td>
          <a href="/cards/<%= card.id %>" class="btn btn-default">Show</a>
          <a href="/cards/<%= card.id %>/edit" class="btn btn-default">Edit</a>
          <a href="/delete_card/<%= card.id %>" class="btn btn-default" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
